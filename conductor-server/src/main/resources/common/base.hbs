<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A ticket management system">
    <meta name="author" content="Santanu Sinha">
    <link rel="icon" href="/favicon.ico">

    <title>CONDUCTOR</title>

    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <!--    <link href="/open-iconic-master/font/css/open-iconic-bootstrap.css" rel="stylesheet">-->
    <link href="/css/dataTables.bootstrap5.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    {{#block "page_css_includes"}}

    {{/block}}
    <link href="/css/conductor.css" rel="stylesheet">

    <style>
        {{#block "page_css"}}

    {{/block}}


    </style>
</head>
<body>
<header>
    <nav class="container-fluid navbar navbar-expand-lg bg-primarycontainer-xxl flex-wrap flex-lg-nowrap bg-primary" aria-label="Main navigation" data-bs-theme="dark">
        <button class="navbar-toggler d-flex d-lg-none order-3 p-2 me-2" type="button" data-bs-toggle="offcanvas"
                data-bs-target="#main-menu" aria-controls="main-menu" aria-label="Toggle navigation">
            <span><i class="bi bi-list"></i> </span>
        </button>
        <a class="navbar-brand logo-area ms-2" href="/">
            <img src="/images/conductor.png" width="30" height="24" class="d-inline-block align-text-top">
            CONDUCTOR
        </a>
        <div class="offcanvas-lg offcanvas-start flex-grow-1" id="main-menu">
            <div class="offcanvas-header px-4 pb-0">
                <h5 class="offcanvas-title text-white" id="main-menu-label">Conductor</h5>
                <button type="button" class="btn-close" data-bs-theme="dark"
                        data-bs-dismiss="offcanvas" aria-label="Close" data-bs-target="#main-menu"></button>
            </div>
            <div class="offcanvas-body p-4 pt-0 p-lg-0" >
                <ul class="navbar-nav me-auto my-2 my-lg-0" style="--bs-scroll-height: 100px;">
                    <li class="nav-item">
                        <a href="/" class="nav-link active">Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="/tickets/query" class="nav-link">Explore Data</a>
                    </li>
                    <li class="nav-item">
                        <a href="/tickets" class="nav-link">Browse Tickets</a>
                    </li>
                    <li class="nav-item">
                        <a href="/dashboards" class="nav-link">Dashboards</a>
                    </li>
                    <li class="nav-item">
                        <a href="/subjects/search" class="nav-link">Subjects</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Manage
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/manage/schema">Schema Management</a></li>
                            <li><a class="dropdown-item" href="/manage/workflow">Workflow Management</a></li>
                            <li><a class="dropdown-item" href="/manage/groups">Group Management</a></li>
                            <li><a class="dropdown-item" href="/manage/skills">Skill Management</a></li>
                            <li><a class="dropdown-item" href="/actions">Global Action Management</a></li>
                            <li><a class="dropdown-item" href="/manage/reports">Reports</a></li>
                        </ul>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Administration
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/admin/roles">Role Management</a></li>
                            <li><a class="dropdown-item" href="/admin/users/search">User Management</a></li>
                        </ul>
                    </li>
                </ul>
                <ul class="navbar-nav flex-row flex-wrap ms-md-auto me-3">
                    <li class="nav-item float-end" style="display: inline-flex">
                        <form class="d-flex  me-2" role="search">
                            <a class="btn btn-success" role="button" href="/tickets/create"><i class="bi bi-plus-lg" title="Create a new ticket"></i></a>
                        </form>
                        <form class="d-flex" role="search" method="post" action="/tickets/go">
                            <input class="form-control me-2" name="goToTicketId" type="search" placeholder="Ticket #" aria-label="Go" title="Go a ticket">
                        </form>
                    </li>

                    {{#empty objectReference}}
                    {{else}}
                        <li class="nav-item float-end">
                            <a class="nav-link d-none d-md-block" type="button" data-bs-toggle="offcanvas"
                                    data-bs-target="#events" aria-controls="events">Events</a>
                        </li>
                    {{/empty}}

                    <li class="nav-item dropdown dropdown-menu-end float-end me-3">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person-circle ms-3 me-1"></i>  {{currentUser/summary/name}}
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/user">Account Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
                        </ul>
                    </li>
                </ul>




            </div>
        </div>
    </nav>
</header>
<main role="main" class="container-fluid">
    <div class="row">


    </div>
    <!--    <div class="row vh-100 overflow-auto">
        <div class="col-12 col-sm-3 col-xl-2 px-sm-2 px-0 bg-dark d-flex sticky-top">
            <div class="d-flex flex-sm-column flex-row flex-grow-1 align-items-center align-items-sm-start px-3 pt-2 pb-2 text-white">
                &lt;!&ndash;                    <a href="/" class="d-flex align-items-center pb-sm-3 mb-md-0 me-md-auto text-white text-decoration-none">&ndash;&gt;
                &lt;!&ndash;                        <span class="fs-5">B<span class="d-none d-sm-inline">rand</span></span>&ndash;&gt;
                &lt;!&ndash;                    </a>&ndash;&gt;
                <a class="d-flex align-items-center pb-sm-3 mb-md-0 me-md-auto text-white  text-decoration-none" href="/" style="font-size: x-large;"><img
                        src="/images/conductor.png" width="45" height="45" alt=""><span class="ms-1">CONDUCTOR</span></a>
                <ul class="nav nav-pills flex-sm-column flex-row flex-nowrap flex-shrink-1 flex-sm-grow-0 flex-grow-1 mb-sm-auto mb-0 justify-content-center align-items-center align-items-sm-start" id="menu">
                    <li class="nav-item">
                        <a href="/" class="fs-5 nav-link px-sm-0 px-2 text-white">
                            <i class="bi fs-3 bi-house"></i><span class="ms-1 d-none d-sm-inline">Home</span>
                        </a>
                    </li>

&lt;!&ndash;                    <li>
                        <a href="/tickets" class="fs-5 nav-link px-sm-0 px-2  text-white">
                            <i class="bi fs-3 bi-ticket"></i><span class="ms-1 d-none d-sm-inline">Tickets</span></a>
                    </li>&ndash;&gt;
                    <li class="dropdown">
                        <a href="#" class="fs-5 nav-link dropdown-toggle px-sm-0 px-2  text-white" id="dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi fs-3 bi-ticket"></i><span class="ms-1 d-none d-sm-inline">Tickets</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark text-small shadow" aria-labelledby="dropdown">
                            <li><a class="dropdown-item" href="/tickets/create">Create</a></li>
                            <li><a class="dropdown-item" href="/">All tickets</a></li>
                        </ul>
                    </li>
                    <li>
                        <a href="/subjects" class="fs-5 nav-link px-sm-0 px-2  text-white">
                            <i class="bi fs-3 bi-people"></i><span class="ms-1 d-none d-sm-inline">Subjects</span></a>
                    </li>
                    <li>
                        <a href="/analytics" class="fs-5 nav-link px-sm-0 px-2  text-white">
                            <i class="bi fs-3 bi-bar-chart"></i><span class="ms-1 d-none d-sm-inline">Analytics</span></a>
                    </li>
                    <li class="dropdown">
                        <a href="#" class="fs-5 nav-link dropdown-toggle px-sm-0 px-2  text-white" id="dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi fs-3 bi-gear-wide-connected"></i><span class="ms-1 d-none d-sm-inline">Administration</span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark text-small shadow" aria-labelledby="dropdown">
                            <li><a class="dropdown-item" href="/admin/roles">Roles</a></li>
&lt;!&ndash;                            <li>
                                <hr class="dropdown-divider">
                            </li>&ndash;&gt;
                            <li><a class="dropdown-item" href="/admin/groups">Groups</a></li>
                            <li><a class="dropdown-item" href="/admin/users/search">Users</a></li>
                            <li><a class="dropdown-item" href="/manage/schema">Schema Management</a></li>
                            <li><a class="dropdown-item" href="/manage/workflow">Workflow Management</a></li>
                        </ul>
                    </li>
&lt;!&ndash;                    <li>
                        <a href="#" class="nav-link px-sm-0 px-2">
                            <i class="fs-5 bi-grid"></i><span class="ms-1 d-none d-sm-inline">Products</span></a>
                    </li>
                    <li>
                        <a href="#" class="nav-link px-sm-0 px-2">
                            <i class="fs-5 bi-people"></i><span class="ms-1 d-none d-sm-inline">Customers</span> </a>
                    </li>&ndash;&gt;
                </ul>
                <div class="dropdown py-sm-4 mt-sm-auto ms-auto ms-sm-0 flex-shrink-1">
                    <a href="#" class="fs-4 d-flex align-items-center text-white text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                        &lt;!&ndash;                            <img src="https://github.com/mdo.png" alt="hugenerd" width="28" height="28" class="rounded-circle">&ndash;&gt;
                        {{#with currentUser}}
        {{#with summary}}
                                <i class="bi bi-person-circle"></i><span class="d-none d-sm-inline mx-1">{{name}}</span>
                            {{/with}}
    {{/with}}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark text-small shadow" aria-labelledby="dropdownUser1">
                        <li><a class="dropdown-item" href="#">Profile</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item" href="#">Sign out</a></li>
                    </ul>
                </div>
            </div>
        </div>-->
    {{#empty objectReference}}
    {{else}}
        <div class="offcanvas offcanvas-end" tabindex="-1" id="events" aria-labelledby="eventsLabel">
            <div class="offcanvas-header">
                <h5 class="offcanvas-title" id="eventsLabel">Events</h5>
                <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
            </div>
            <div class="offcanvas-body">
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>Type</th>
                        <th>Date</th>
                        <th>User</th>
                    </tr>
                    </thead>
                    <tbody id="events-list">

                    </tbody>
                </table>
            </div>
        </div>
    {{/empty}}

    <div class="row container-fluid m-2">
        <div class="col-sm-12">
            {{#block "content"}}

            {{/block}}
        </div>
    </div>
    <div class="toast-container position-fixed bottom-0 end-0 p-3">
        <div class="toast" role="alert" aria-live="assertive" aria-atomic="true" id="error-toast">
            <div class="toast-header">
                <!--            <img src="..." class="rounded me-2" alt="...">-->
                <i class="bi bi-exclamation-octagon-fill text-danger me-2"></i>
                <strong class="me-auto text-danger">Error</strong>
                <!--            <small>11 mins ago</small>-->
                <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                There was an error
            </div>
        </div>
    </div>

</main>
<script src="/js/swal.js"></script>
<script src="/js/jquery-3.7.0.min.js"></script>
<script src="/js/bootstrap.bundle.min.js"></script>
<script type="text/javascript" src="/js/jquery.dataTables.min.js"></script>
<script type="text/javascript" src="/js/dataTables.bootstrap5.min.js"></script>
<script src="/js/cookies.js"></script>

{{#block "page_js_includes"}}

{{/block}}
<script type="application/javascript">
    (function() {
      'use strict';
      $.fn.dataTable.ext.errMode = 'none';

        $('#table').on( 'error.dt', function ( e, settings, techNote, message ) {
            console.log( 'An error has been reported by DataTables: ', message );
        } ) ;

      window.addEventListener( "pageshow", function ( event ) {
        var historyTraversal = event.persisted ||
                               ( typeof window.performance != "undefined" &&
                                    window.performance.navigation.type === 2 );
        if ( historyTraversal ) {
          // Handle page restore.
          window.location.reload();
        }
      });


      window.addEventListener('load', function() {
        // Fetch all the forms we want to apply custom Bootstrap validation styles to
        var forms = document.getElementsByClassName('needs-validation');
        // Loop over them and prevent submission
        var validation = Array.prototype.filter.call(forms, function(form) {
          form.addEventListener('submit', function(event) {
            if (form.checkValidity() === false) {
              event.preventDefault();
              event.stopPropagation();
            }
            form.classList.add('was-validated');
          }, false);
        });
      }, false);
    })();

    $(document).ready(function() {
    {{#block "page_js"}}

    {{/block}}

    $( ".dataTables_paginate" ).each(function( index ) {
      $( this ).parent().addClass('col-md-7').removeClass('col-md-9')
    });

    $( ".dataTables_filter" ).each(function( index ) {
      $( this ).parent().addClass('col-md-3').removeClass('col-md-1')
    });
    const error = $.cookie("server-error-message");
    if(error) {
        const toastElement = document.getElementById("error-toast")
        const toast = bootstrap.Toast.getOrCreateInstance(toastElement);
        $(toastElement).find(".toast-body").text(error);
        $.removeCookie("server-error-message", { path: '/' });
        toast.show();
    }

    function timeSince(data, type, row, meta) {
        return '<span title="' + new Date(data) + '">' + timeSinceString(data, type, row, meta) +  '</span>';
    }

    function timeSinceString(data, type, row, meta) {
        if(type === "sort") return data;

        var date = new Date(data);

        var seconds = Math.floor((new Date() - date) / 1000);

        var interval = Math.floor(seconds / 31536000);

        if (interval > 1) {
            return interval + " years";
        }
        interval = Math.floor(seconds / 2592000);
        if (interval > 1) {
            return interval + " months";
        }
        interval = Math.floor(seconds / 86400);
        if (interval > 1) {
            return interval + " days";
        }
        interval = Math.floor(seconds / 3600);
        if (interval > 1) {
            return interval + " hours";
        }
        interval = Math.floor(seconds / 60);
        if (interval > 1) {
            return interval + " minutes";
        }
        return Math.floor(seconds);
    }

    function lifetime(data, type, row, meta) {
        if(type === "sort") return data;

        var created = new Date(row['created']);
        var updated = new Date(row['updated']);

        var seconds = Math.floor((updated - created) / 1000);

        var interval = Math.floor(seconds / 31536000);

        if (interval > 1) {
            return interval + " years";
        }
        interval = Math.floor(seconds / 2592000);
        if (interval > 1) {
            return interval + " months";
        }
        interval = Math.floor(seconds / 86400);
        if (interval > 1) {
            return interval + " days";
        }
        interval = Math.floor(seconds / 3600);
        if (interval > 1) {
            return interval + " hours";
        }
        interval = Math.floor(seconds / 60);
        if (interval > 1) {
            return interval + " minutes";
        }
        return Math.floor(seconds) + " seconds";
    }

    function formatBytes(data, type, row, meta) {
        if(type === 'display') {
            const decimals = 2;
            const bytes = data * 1024 * 1024;
            if (bytes === 0) return '0 Bytes';

            const k = 1024;
            const dm = decimals < 0 ? 0 : decimals;
            const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB', 'PB', 'EB', 'ZB', 'YB'];

            const i = Math.floor(Math.log(bytes) / Math.log(k));

            return parseFloat((bytes / Math.pow(k, i)).toFixed(dm)) + ' ' + sizes[i];
        }
        return data;
    }

    function loadEventData(objectType, objectId) {
        const data = $.get("/events/list/object/" + objectType + (objectId ? ("/" + objectId) : "")
                    + ($('#events-list').attr('nextPage') ? "?next=" + $('#events-list').attr('nextPage') : ""),
                function(res, textStatus, request) {
                    if(res.length > 0) {
                        $('#events-list').append(res);
                    }
                    $('#events-list').attr('nextPage', request.getResponseHeader("next-pointer"));
                });
        console.log(data);
    }

    //Sets up data table with external search box
    $('.data-table').each(function() {
        const tableElement = $(this);
        const table = tableElement.DataTable({
            paging: false,
            info: false,
            responsive: true,
            "dom":'t'
        });
        const searchBoxName = tableElement.attr('data-table-search');
        if(searchBoxName) {
            const searchBox = $(searchBoxName);
            if(searchBox) {
                searchBox.on('keyup', function(e) {
                    const text = $(this).val();
                    table.search(text).draw();
                });
            }
        }
    });


	$('.post-button').on('click', function(e) {
	    e.preventDefault();
	    var data = new Object();
	    data[this.attr('data-field')] = this.attr('data-submit');
	    $.post(this.href, data, function() {
	        alert('Action complete');
	        location.reload();
	    });
	});

    $('form.confirmed-form').submit(function (e, params) {
        var localParams = params || {};

        if (!localParams.send) {
            e.preventDefault();

            var form = $(this);
            Swal.fire({
                    title: form.attr('data-cnf-title'),
                    text: form.attr('data-cnf-text'),
                    type: "warning",
                    showCancelButton: true,
                    focusConfirm: false,
                    confirmButtonColor: "#6A9944",
                    confirmButtonText: "Confirm",
                    cancelButtonText: "Cancel",
                    closeOnConfirm: true
                }).then(function (result) {
                    if (result.value) {
                        $(e.currentTarget).trigger(e.type, { 'send': true });
                    }
                });
        }
    });

    {{#empty objectReference}}
    {{else}}
        const events = $('#events')
        events.on('show.bs.offcanvas', event => {
            $('#events-list').empty();
            $('#events-list').attr('nextPage', '');
          loadEventData("{{objectReference.type}}", "{{objectReference.objectId}}");
        });
    {{/empty}}



});


</script>
</body>
</html>